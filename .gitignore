# Ignore files generated by the IDE
.idea/

# Ignore compiled binaries
*.exe
*.dll

# Ignore log files
*.log

# Ignore package files
*.zip
*.tar.gz

# Ignore node_modules directory
node_modules/

# ignore IARCImageBankCOlpo and IARCImageBankVIA
IARCImageBankCOlpo/
IARCImageBankVIA/

# Ignore the .vscode folder
.vscode/

# Ignore the .gitignore file itself
!.gitignore
```

## 4. Push your changes to the repository

```sh
git add .gitignore
git commit -m "Add .gitignore file"
git push origin master
```

## 5. Pull the changes from the repository

```sh
git pull origin master
```

# Git Branching

## 1. Create a new branch

```sh
git checkout -b feature-branch
```

## 2. Make changes to the files

## 3. Add and commit the changes to the new branch

```sh
git add .
git commit -m "Add feature-branch"
```

## 4. Push the changes to the repository

```sh
git push origin feature-branch
```

## 5. Merge the changes to the master branch

```sh
git checkout master
git merge feature-branch
```

## 6. Push the changes to the repository

```sh
git push origin master
```

## 7. Delete the feature branch

```sh
git branch -d feature-branch
```

# Git Pull

## 1. Pull the changes from the repository

```sh
git pull origin master
```

# Git Clone

## 1. Clone the repository

```sh
git clone